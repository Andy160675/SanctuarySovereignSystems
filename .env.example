# Sovereign System Environment Configuration
# Copy this file to .env and customize for your environment

# =============================================================================
# COST CONTROLS
# =============================================================================
# Maximum daily cost limit for automated operations (USD)
GLOBAL_MAX_DAILY_COST=50.0

# =============================================================================
# DEPLOYMENT TRACK
# =============================================================================
# Evidence track: "insider" (development) or "stable" (production)
EVIDENCE_TRACK=stable

# =============================================================================
# EXTERNAL ANCHORING (Optional)
# =============================================================================
# RFC3161 Timestamp Authority URL (leave empty to disable)
# RFC3161_TSA_URL=http://timestamp.digicert.com

# IPFS Gateway (leave empty to disable)
# IPFS_GATEWAY_URL=https://ipfs.io

# Arweave Gateway (leave empty to disable)
# ARWEAVE_GATEWAY_URL=https://arweave.net

# =============================================================================
# PUBLIC SAFE MODE
# =============================================================================
# Enable for public deployments (restricts automation, disables external anchors)
# PUBLIC_SAFE_MODE=false

# Block all automation in public safe mode
# BLOCK_AUTOMATION_IN_PUBLIC_SAFE_MODE=true

# Allowed handlers in public safe mode (comma-separated)
# SAFE_ALLOWED_HANDLERS=VERIFY_CHAIN_NOW

# =============================================================================
# API KEYS (Keep Secret!)
# =============================================================================
# Add any API keys here - NEVER commit actual values to git
# GEMINI_API_KEY=your-key-here
# OPENAI_API_KEY=your-key-here

# =============================================================================
# AUTHENTICATION (NEW)
# =============================================================================
# API key for external access to protected endpoints (kill-switch, boardroom, etc.)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SOVEREIGN_API_KEY=your-secure-api-key-here

# Enable/disable authentication (set to "false" for local development only)
AUTH_ENABLED=true

# Service-to-service authentication key (internal mesh communication)
SOVEREIGN_SERVICE_KEY=your-internal-service-key-here

# =============================================================================
# CONNECTION ENDPOINTS
# =============================================================================
# Truth Engine API URL (default: http://localhost:5050)
TRUTH_ENGINE_URL=http://localhost:5050

# Ollama Host URL (default: http://localhost:11434)
OLLAMA_HOST=http://localhost:11434

# Boardroom Shell (Web/Electron) Port
BOARDROOM_PORT=3000
